import 'package:flutter/material.dart';

class PageRilis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Versi 1.0.0",
          style: TextStyle(fontFamily: 'Childern', fontSize: 20, color: Colors.green),),
      ),
    );
  }
}
